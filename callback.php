<?php
//echo phpinfo();exit;
// echo "21212";exit;
    /** 定义 */
    define ('APP_PATH', __DIR__);
    define ('APP_URL', rtrim (dirname ($_SERVER['SCRIPT_NAME']), '/'));
   //  $redis = new Redis(); 
   // if($redis->connect('tokenman-001.zsh657.0001.use2.cache.amazonaws.com', 6379)) 
   // {   
   //     echo "success\n";
   // }

   // exit;
    /** 初始化框架 */
    require_once 'System/Init.php';
?>
